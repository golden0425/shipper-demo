import React, { useState } from 'react'
import { connect } from 'react-redux'

const Home = ({ homeData }) => {
  return (
    <>
      <div>首页</div>
    </>
  )
}
// const mapStateToProps = state => ({ homeData: 1 })

// const mapDispatchToProps = dispatch => {}

export default Home
